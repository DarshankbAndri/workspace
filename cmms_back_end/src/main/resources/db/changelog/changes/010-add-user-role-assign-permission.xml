<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="010-add-user-role-assign-permission" author="darshan">
        <sql>
            INSERT INTO permission_master (permission_code, permission_name, module_name, action_name, status, created_at, updated_at)
            VALUES ('USER_ROLE_ASSIGN', 'Assign User Roles', 'Admin', 'ASSIGN', 'ACTIVE', NOW(), NOW())
            ON CONFLICT (permission_code) DO NOTHING;

            INSERT INTO role_permission (role_id, permission_id)
            SELECT r.role_id, p.permission_id
            FROM role_master r
            JOIN permission_master p ON p.permission_code = 'USER_ROLE_ASSIGN'
            WHERE r.role_code IN ('SUPER_ADMIN', 'ADMIN', 'HR_ADMIN')
            ON CONFLICT (role_id, permission_id) DO NOTHING;
        </sql>
    </changeSet>
</databaseChangeLog>
