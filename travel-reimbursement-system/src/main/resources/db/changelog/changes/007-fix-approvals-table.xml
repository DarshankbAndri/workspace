<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Fix approvals table: add role column, rename approval_date to approved_at, drop updated_at -->
    <changeSet id="007-fix-approvals-table" author="admin">
        <!-- Add missing role column -->
        <addColumn tableName="approvals">
            <column name="role" type="user_role_enum" defaultValue="MANAGER">
                <constraints nullable="false"/>
            </column>
        </addColumn>
        
        <!-- Rename approval_date to approved_at -->
        <renameColumn tableName="approvals" oldColumnName="approval_date" newColumnName="approved_at"/>
        
        <!-- Drop updated_at column (not in Approval entity) -->
        <dropColumn tableName="approvals" columnName="updated_at"/>
    </changeSet>

</databaseChangeLog>
